import { Router } from 'express';

const clientRouter = new Router();

// Add routes
// clientRouter.get('/', SessionController.store);
// clientRouter.post('/', SessionController.store);
// clientRouter.put('/', SessionController.store);
// clientRouter.delete('/', SessionController.store);

export default clientRouter;
